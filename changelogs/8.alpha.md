# 8.alpha

- iOS 26 tall clock
- Remove Widget Background integration (cre OwnGoalStudio for the source code)
- applied glass to the lockscreen passcode screen
- you can now import and export preferences (bottom of the More Options subpage)
- rebuilt the settings modernization pass with liquid sliders, liquid switches, liquid segmented controls, rounded rows, and the top gradient fade
- added per-page reset in prefs
- added dynamic stock corner radius default caching so reset now goes back to the fetched system radius instead of old hardcoded fallback values
- hardened the widget hook so it no longer injects into some fullscreen blur source (idk why it even exists)
- quick actions live capture now dont crash springboard
- forced lockscreen quick actions into light appearance to avoid unreadable dark glyph rendering for some users
- improved legacy wallpaper and snapshot handling across older iOS paths (simpler fixed cpbitmap sourcing and safer cache refresh)
- improved app library source alignment on fullscreen phones and fixed several search field and glass radius funny cases
- big internal cleanup pass across the runtime, prefs, and snapshot systems improved performance
- several refresh paths were improved so wallpaper, blur, and snapshot work does less unnecessary repeat work
- shared runtime plumbing was cleaned up so display links, caching, and log handling behave more predictably
- many smaller polish, localization, and bug fixes applied across springboard, prefs, widgets, app library, lockscreen, and rwb
